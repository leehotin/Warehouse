<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Stock Enquiry</title>
</head>
<body>
    <h1>Stock Enquiry</h1>
    <form action="/stock" method="post">
        <input type="text" name="_id" value="<%= stock_data._id %>" hidden>
        <div>
            <div>
                <label for="stock_id">Stock ID:</label>
            </div>
            <div>
                <input type="text" id="stock_id" name="stock_id" value="<%= stock_data.stock_id %>" required>
            </div>
        </div>
        <div>
            <div>
                <label for="area">Region : </label> 
            </div>
            <div>
                <input type="text" id="area" name="area" value="<%= stock_data.area %>" required> 
            </div>
        </div>
        <div>
            <div>
                <label for="name"> Store location : </label>
            </div>
            <div>
                <input type="text" id="name" name="name" value="<%= stock_data.name %>" required>
            </div>
        </div>
        
        <button type="submit"> Store at </button>
    </form>

    <!-- Display stock information here -->
    <div id="stock-info">
        <!-- Stock ID, Region, Stock Location will be populated dynamically -->
    </div>
</body>
</html>